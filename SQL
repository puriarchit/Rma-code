SELECT * FROM dbo.NegativeList_History_Summary;




SELECT WLType, COUNT(*) AS RecordCount
FROM dbo.NegativeList WITH (NOLOCK)
GROUP BY WLType
ORDER BY RecordCount DESC;




SELECT SUM(rows) 
FROM sys.partitions 
WHERE object_id = OBJECT_ID('dbo.NegativeList') 
  AND index_id IN (0, 1);
